<%= form_for(@employee_constraint) do |f| %>
  <% if @employee_constraint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_constraint.errors.count, "error") %> prohibited this employee_constraint from being saved:</h2>

      <ul>
      <% @employee_constraint.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
