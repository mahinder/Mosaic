<h1>Listing employee_constraints</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employee_constraints.each do |employee_constraint| %>
  <tr>
    <td><%= link_to 'Show', employee_constraint %></td>
    <td><%= link_to 'Edit', edit_employee_constraint_path(employee_constraint) %></td>
    <td><%= link_to 'Destroy', employee_constraint, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employee constraint', new_employee_constraint_path %>
