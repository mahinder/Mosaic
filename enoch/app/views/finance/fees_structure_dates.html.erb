
<div class="block-border" id="outer_block">
	<div class="block-content form">
  <h1>Fees Structure</h1>
       <div class="block-controls enoch_block">
			<ul class="controls-tabs js-tabs">
				<li >
					<%= link_to image_tag("/assets/icons/web-app/32/Back.png"),"javascript:history.back()" %>
				</li>
			</ul>
		</div>  
<fieldset>
	<legend><a href="#">Fees Detail of <%= @student.full_name%></a></legend>
<div class="columns">
	<p class="colx-left required select-fixed-width1">
		<label for="Collection date"> Collection Date</label>
		 <%= select  :fees_submission_detail, :dates_id, @dates.map { |e| [e.full_name, e.id]}, {:prompt => "Select"},:onchange => "fee_structure_for_student(#{@student.id},this)" %>
	</p>
</div>
<!-- 


  <div class="label-field-pair">
    <label>Select Fee Collection date: </label>
  <div class="text-input-bg">
    <!-- <%#= select  :fees_submission, :dates_id, @dates.map { |e| [e.full_name, e.id]},
      {:prompt => "Select fees collection date"},
      {:onChange => "#{remote_function( :url =>
      {:action => "fees_structure_for_student"},
      :with => "'date='+value+'&id='+#{@student.id}") }"} %> 
  </div>
  </div> -->
<div id= "fees_structure_Of_Student"></div>

<div id="modal-box" style="display:none;"></div>
</fieldset>
	</div>
</div>