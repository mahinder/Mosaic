<%= form_for(@grading_level_detail) do |f| %>
  <% if @grading_level_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grading_level_detail.errors.count, "error") %> prohibited this grading_level_detail from being saved:</h2>

      <ul>
      <% @grading_level_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
