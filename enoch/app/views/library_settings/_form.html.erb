<%= form_for(@library_setting) do |f| %>
  <% if @library_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@library_setting.errors.count, "error") %> prohibited this library_setting from being saved:</h2>

      <ul>
      <% @library_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
