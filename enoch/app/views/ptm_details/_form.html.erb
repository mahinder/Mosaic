<%= form_for(@ptm_detail) do |f| %>
  <% if @ptm_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ptm_detail.errors.count, "error") %> prohibited this ptm_detail from being saved:</h2>

      <ul>
      <% @ptm_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
