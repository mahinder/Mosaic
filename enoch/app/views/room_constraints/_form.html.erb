<%= form_for(@room_constraint) do |f| %>
  <% if @room_constraint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_constraint.errors.count, "error") %> prohibited this room_constraint from being saved:</h2>

      <ul>
      <% @room_constraint.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
