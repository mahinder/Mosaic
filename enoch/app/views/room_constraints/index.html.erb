<h1>Listing room_constraints</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @room_constraints.each do |room_constraint| %>
  <tr>
    <td><%= link_to 'Show', room_constraint %></td>
    <td><%= link_to 'Edit', edit_room_constraint_path(room_constraint) %></td>
    <td><%= link_to 'Destroy', room_constraint, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Room constraint', new_room_constraint_path %>
