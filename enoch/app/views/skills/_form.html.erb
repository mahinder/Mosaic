<%= form_for(@mandatory_skill) do |f| %>
  <% if @mandatory_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mandatory_skill.errors.count, "error") %> prohibited this mandatory_skill from being saved:</h2>

      <ul>
      <% @mandatory_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
