<%= form_for(@student_co_scholastic_area_assessment_detail) do |f| %>
  <% if @student_co_scholastic_area_assessment_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_co_scholastic_area_assessment_detail.errors.count, "error") %> prohibited this student_co_scholastic_area_assessment_detail from being saved:</h2>

      <ul>
      <% @student_co_scholastic_area_assessment_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
