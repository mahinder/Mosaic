<%= form_for(@sub_skill) do |f| %>
  <% if @sub_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_skill.errors.count, "error") %> prohibited this sub_skill from being saved:</h2>

      <ul>
      <% @sub_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
